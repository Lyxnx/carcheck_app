<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="net.lyxnx.reginfo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
            android:allowBackup="false"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Main"
            tools:ignore="GoogleAppIndexingWarning">

        <activity
                android:name=".activity.MainActivity"
                android:windowSoftInputMode="stateHidden|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.calculators.CalculatorsActivity"/>
        <activity android:name=".activity.calculators.JourneyCostActivity"/>
        <activity android:name=".activity.calculators.FinanceActivity"/>
        <activity android:name=".activity.calculators.FuelCostActivity"/>
        <activity android:name=".activity.MOTInfoActivity"/>
        <activity android:name=".activity.VehicleInfoActivity"/>
        <activity android:name=".activity.HistoryActivity"/>
    </application>
</manifest>